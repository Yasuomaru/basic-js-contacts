import { ContactFullNameValidator } from "../FullnameValidator.js"

class ContactValidator{
    static exec(fullname){
        ContactFullNameValidator.exec(fullname)
    }
}
export default ContactValidator